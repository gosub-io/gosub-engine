[package]
name = "gosub_taffy"
version = "0.1.1"
edition = "2021"
authors = ["Gosub Community <info@gosub.io>"]
license = "MIT"
description = "Taffy layouter for Gosub"

[dependencies]
gosub_shared = { version = "0.1.1", registry = "gosub", path = "../gosub_shared" }
gosub_interface = { version = "0.1.1", registry = "gosub", path = "../gosub_interface" }
taffy = "0.7.1"
anyhow = "1.0.94"
regex = "1.11.1"
log = "0.4.22"
parley = { version = "0.2.0", default-features = false, features = ["std"] }
