//! Render backend contract types. They moved to `gosub_interface::render::backend` so that
//! `ModuleConfiguration` can reference them; re-exported here to keep existing paths working.

pub use gosub_interface::render::backend::*;
